import {ITopping} from '../model/topping';

export var toppings: Array<ITopping> = [];

toppings.push({
    label: 'Rocklets',
    imgURL: ''
});
toppings.push({
    label: 'Chispas de chocolate',
    imgURL: ''
});
toppings.push({
    label: 'Gomitas',
    imgURL: ''
});
